# 🏗️ ยิ่งสูงยิ่งหนาว (Higher is Colder) - Tower Scoreboard

เว็บแอปพลิเคชันสำหรับแสดงผลคะแนนการแข่งขันในรูปแบบ **"ตึกระฟ้า"** ยิ่งคะแนนเยอะ ตึกยิ่งสูง! พร้อมเอฟเฟกต์หิมะเกาะยอดตึกและมงกุฎสำหรับผู้ชนะ

![Project Screenshot](https://via.placeholder.com/800x400?text=Higher+is+Colder+Preview) 
*(คุณสามารถเอารูปหน้าจอจริงมาใส่แทนลิงก์ข้างบนนี้ได้)*

## ✨ ฟีเจอร์หลัก (Features)
- **Visualized Score:** แปลงตัวเลขคะแนนเป็นความสูงของตึกแบบ Real-time
- **Dynamic Height:** คำนวณความสูงเปรียบเทียบกับผู้ที่ได้คะแนนสูงสุด (Relative Scale)
- **Winner Crown:** ทีมที่มีคะแนนสูงสุดจะได้รับ "ตึกสีทอง" และ "มงกุฎ 👑" โดยอัตโนมัติ
- **Snow Effect:** เมื่อความสูงเกิน 80% จะมีหิมะเกาะที่ยอดตึกและตัวเลขคะแนน ❄️
- **Flexible Display:** รองรับการแสดงผลเต็มจอ (Full Width) ชื่อทีมแสดงชัดเจนบนตัวตึก
- **Dual Mode:** รองรับทั้งการใช้งานผ่านเน็ต (Google Sheets) และ ไม่ใช้เน็ต (Offline Admin)

---

## 🚀 วิธีการใช้งาน (How to use)

โปรเจกต์นี้รองรับ 2 โหมดการทำงาน เลือกใช้ตามสถานการณ์:

### 📡 โหมด 1: Online (เชื่อมต่อ Google Sheets)
เหมาะสำหรับงานที่มีอินเทอร์เน็ต และต้องการแก้คะแนนผ่านมือถือหรือคอมเครื่องอื่น

1. **เตรียม Google Sheets:**
   - สร้าง Sheet ใหม่ โดยมี 3 คอลัมน์: `ID` (1-9), `Name` (ชื่อทีม), `Score` (คะแนน)
   - **สำคัญ:** ตั้งค่า Format ของคอลัมน์ `Name` ให้เป็น **Plain Text (ข้อความธรรมดา)**
   - ไปที่ `File` > `Share` > `Publish to web` > เลือกเป็น **CSV**

2. **ตั้งค่าในโค้ด:**
   - เปิดไฟล์ `index.html`
   - ค้นหาตัวแปร `const SHEET_ID = '...'`
   - นำ ID ของ Google Sheets (ตัวอักษรยาวๆ ระหว่าง `/d/` กับ `/edit`) มาใส่

3. **รันโปรแกรม:**
   - เปิดไฟล์ `index.html` ผ่าน **Live Server** (แนะนำ) หรืออัปโหลดขึ้น GitHub Pages

---

### 🔌 โหมด 2: Offline (ไม่มีอินเทอร์เน็ต)
เหมาะสำหรับออกบูธ หรือสถานที่ที่เน็ตไม่เสถียร ใช้คอมพิวเตอร์เครื่องเดียวจบ

1. **เปิดไฟล์:**
   - เปิด `index.html` (หน้าจอแสดงผล) บน Browser
   - เปิด `admin.html` (หน้าควบคุม) บน Browser **(ต้องเป็น Browser ตัวเดียวกัน)**

2. **การควบคุม:**
   - กรอกชื่อทีมและคะแนนในหน้า `admin.html`
   - กดปุ่ม **"💾 อัปเดตคะแนนทันที"**
   - หน้าจอ `index.html` จะขยับเปลี่ยนตามทันทีโดยไม่ต้อง Refresh

---

## 📂 โครงสร้างไฟล์ (File Structure)

```text
├── index.html       # หน้าจอแสดงผลกราฟ (Display)
├── admin.html       # หน้าจอควบคุมคะแนนสำหรับโหมด Offline
└── README.md        # เอกสารคู่มือการใช้งาน
```

🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
HTML5 & CSS3 (Flexbox, CSS Transitions, Animations)

JavaScript (Vanilla) (Fetch API, LocalStorage)

Google Visualization API (สำหรับดึงข้อมูลจาก Sheets CSV)